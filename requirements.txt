# director-signals - Dependencies
# Core scientific computing and signal processing

# Numerical operations and array processing
numpy>=1.21.0

# Signal processing, statistics, and peak finding
scipy>=1.7.0

# Plotting and visualization
matplotlib>=3.4.0

# Audio loading convenience (optional but recommended)
# Fallback to scipy.io.wavfile if not available
librosa>=0.9.0

# Required for linear interpolation resampling (C++ parity)
samplerate>=0.2.0

# Testing framework
pytest>=7.0.0
